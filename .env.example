# Copy this file to ".env" for local testing, OR add these as
# Environment Variables in Vercel (Project → Settings → Environment Variables).
# NEVER commit the real connection string — .env is gitignored.

# MongoDB Atlas connection string (Atlas → Connect → Drivers → Node.js)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Optional — database name (defaults to "fathersday")
MONGODB_DB=fathersday

# Optional — turn the "Write it for me" button into REAL AI (Groq — free).
# Get a key at console.groq.com/keys. Without it, the site uses built-in templates.
GROQ_API_KEY=gsk_xxxxxxxx

# Optional — Razorpay makes payment REQUIRED (link unlocks only after a
# verified payment). Get keys at dashboard.razorpay.com → Settings → API Keys.
# Without these, the site falls back to the simple (trust-based) UPI flow.
RAZORPAY_KEY_ID=rzp_test_xxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxx
